import type { AssessmentTask } from "./types";

/**
 * Assessment content belongs here (or in adjacent structured data modules),
 * never inline inside presentation components.
 */
export const assessmentTasks: AssessmentTask[] = [];
